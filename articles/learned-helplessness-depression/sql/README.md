# SQL Schema

Example with SQLite:

```bash
sqlite3 learned_helplessness_research.db < learned_helplessness_schema.sql
```

The schema separates participants, domains, waves, observation-level learned-helplessness indicators, context audits, and item-bank responses so research teams can document both agency dynamics and surrounding support/barrier conditions.
