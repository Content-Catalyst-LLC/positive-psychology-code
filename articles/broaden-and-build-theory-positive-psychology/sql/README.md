# SQL Schema

Example with SQLite:

```bash
sqlite3 broaden_build_research.db < broaden_build_schema.sql
```

The schema separates participants, conditions, waves, observation-level indicators, emotion-episode notes, and practice-quality audits so research teams can document both outcomes and mechanisms.
