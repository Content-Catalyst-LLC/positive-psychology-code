# SQL Schema

Example with SQLite:

```bash
sqlite3 meaning_purpose_research.db < meaning_purpose_schema.sql
```

The schema separates participants, domains, waves, observation-level meaning/purpose indicators, institutional-context audits, and item-bank responses so research teams can document both meaning-system dynamics and surrounding support/barrier conditions.
