# SQL Schema

Example with SQLite:

```bash
sqlite3 sdt_research.db < sdt_schema.sql
```

The schema separates participants, domains, waves, observation-level SDT indicators, motivational-climate audits, and item-bank responses so research teams can document both motivational outcomes and institutional context.
