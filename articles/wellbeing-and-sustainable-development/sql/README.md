# SQL Schema

Example with SQLite:

```bash
sqlite3 wellbeing_sustainable_development.db < wellbeing_sustainable_development_schema.sql
```

The schema separates countries, years, observation-level indicators, and indicator-bank responses so research teams can document both composite indicators and domain-level measures.
