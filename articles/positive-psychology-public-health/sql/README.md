# SQL Schema

Example with SQLite:

```bash
sqlite3 positive_psychology_public_health.db < positive_psychology_public_health_schema.sql
```

The schema separates regions, years, observation-level indicators, and indicator-bank responses so research teams can document both composite public-health well-being indicators and domain-level measures.
