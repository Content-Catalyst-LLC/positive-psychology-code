# SQL Schema

Example with SQLite:

```bash
sqlite3 ptg_research.db < ptg_schema.sql
```

The schema separates participants, trauma contexts, waves, observation-level PTG indicators, context-support audits, and item-bank responses so research teams can document both PTG process dynamics and surrounding support/barrier conditions.
