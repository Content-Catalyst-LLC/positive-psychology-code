# SQL Schema

Example with SQLite:

```bash
sqlite3 cultural_wellbeing.db < cultural_wellbeing_schema.sql
```

The schema separates cultural groups, participants, waves, observation-level indicators, and item-level responses so that research teams can document both composite indicators and measurement item banks.
