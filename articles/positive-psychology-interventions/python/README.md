# Python Professional Workflow

Run:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install pandas numpy scikit-learn networkx matplotlib
python ppi_professional_workflow.py
```

Outputs include reliability reports, practice-quality audits, mechanism indices, PCA summaries, network centrality, edge tables, bootstrap stability summaries, and a network figure.
