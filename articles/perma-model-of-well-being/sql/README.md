# SQL Schema

Example with SQLite:

```bash
sqlite3 perma_research.db < perma_schema.sql
```

The schema separates participants, settings, waves, observation-level PERMA indicators, context audits, and item-bank responses so research teams can document both well-being profiles and surrounding support/barrier conditions.
