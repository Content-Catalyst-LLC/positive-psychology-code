# SQL Schema

Example with SQLite:

```bash
sqlite3 flow_optimal_experience_research.db < flow_schema.sql
```

The schema separates participants, domains, sessions, observation-level flow indicators, attention-context audits, and item-bank responses so research teams can document both flow dynamics and surrounding support/barrier conditions.
