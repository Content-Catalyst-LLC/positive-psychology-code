# SQL Schema

Example with SQLite:

```bash
sqlite3 three_good_things.db < three_good_things_schema.sql
```

The schema separates participants, daily observations, good-thing entries, coded explanations, and practice-quality audits so research teams can document both outcomes and reflective mechanisms.
