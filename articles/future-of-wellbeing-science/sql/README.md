# SQL Schema

Example with SQLite:

```bash
sqlite3 future_wellbeing.db < future_wellbeing_schema.sql
```

The schema separates participants, waves, observation-level indicators, and item-level responses so that research teams can document both composite indicators and psychometric item banks.
