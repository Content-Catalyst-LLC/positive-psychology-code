# SQL Schema

Example with SQLite:

```bash
sqlite3 positive_education.db < positive_education_schema.sql
```

The schema separates schools, students, waves, observation-level indicators, implementation-quality audits, and item-bank responses so research teams can document both student-flourishing patterns and implementation evidence.
